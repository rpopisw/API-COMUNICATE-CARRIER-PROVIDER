const services = {
    carrier:'fake_carrier'
}

const status_label ={
    pending : 'pending',
    processing: 'processing',
    completed : 'completed',
    error: 'error'
}

module.exports = {
    services,
    status_label
}